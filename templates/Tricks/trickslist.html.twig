{% extends 'base.html.twig' %}

{% block body %}
    {#{% for i in 0..2 %}#}
        <div class="d-flex justify-content-around mt-2 ">
            {#{% for i in  0..4 %}#}
                <div class="card mb-3 " style="width: 18rem;">
                    {% for trick in tricks %}
                    <img class="card-img-top img-thumbnail"
                         src="https://image.noelshack.com/fichiers/2018/02/5/1515759259-snowboarders-2089706-1920.jpg"
                         alt="Card image cap">
                    <div class="card-body p-2">
                        <a href="#" class="card-link"><u>{{ trick.title }}</u></a>
                        <a href="#" class="card-link float-right"><i class="fa fa-trash"></i></a>
                        <a href="#" class="card-link float-right"><i class="fa fa-pencil"></i></a>
                    </div>
                        {% endfor %}
                </div>
            {#{% endfor %}#}
        </div>
        </div>
    {#{% endfor %}#}

    <div class="text-center pt-5">
        <button type="submit" class="btn btn-primary"><i class="fa fa-spinner" aria-hidden="true"></i> Load more
        </button>
    </div>
{% endblock %}